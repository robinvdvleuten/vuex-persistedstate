const createPersistedState = require('../dist/vuex-persistedstate')

describe('vuex-persistedstate', function () {
  it('replaces store\'s state when initializing', function () {
    console.log(createPersistedState)
  })

  it('subscribes to store\'s state changes when initializing', function () {
    console.log(createPersistedState)
  })
})
